[project]
name = "nanojp-gpt"
version = "0.1.0"
description = "AutoResearch: Lightweight Japanese GPT training optimization"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.11.0",
    "numpy",
    "datasets",
]

[tool.uv.sources]
torch = { index = "pytorch-cu126" }

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
